const Category = (() => {

    class Category {

        constructor(id, name, parentCategory ) {
            this.id = id;
            this.name = name;
            this.parentCategory = parentCategory;
        }

    }

    return Category;

})();
